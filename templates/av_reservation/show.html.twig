{% extends 'base.html.twig' %}

{% block title %}AvReservation{% endblock %}

{% block body %}
    <h1>AvReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_reservation.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ av_reservation.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ av_reservation.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ av_reservation.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ av_reservation.phone }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ av_reservation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_reservation_index') }}">back to list</a>

    <a href="{{ path('app_av_reservation_edit', {'id': av_reservation.id}) }}">edit</a>

    {{ include('av_reservation/_delete_form.html.twig') }}
{% endblock %}
