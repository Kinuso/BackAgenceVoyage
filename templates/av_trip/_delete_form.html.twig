<form method="post" action="{{ path('app_av_trip_delete', {'id': av_trip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_trip.id) }}">
    <button class="btn">Delete</button>
</form>
